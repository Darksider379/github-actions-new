name: Matrix
on: push

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: log node version 
        run: npm -v 
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: log node version 
        run: npm -v 